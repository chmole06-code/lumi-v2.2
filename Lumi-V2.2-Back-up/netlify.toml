[build]
  base = "Lumi-V2.2-Back-up"
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
